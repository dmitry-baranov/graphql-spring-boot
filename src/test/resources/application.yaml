spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:test-db
    username: sa
    password: pass
    driverClassName: org.h2.Driver
logging:
  level:
    root: debug
